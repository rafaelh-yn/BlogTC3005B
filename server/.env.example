# Servidor
PORT=8000

# Base de datos
DB_HOST=localhost
DB_PORT=5432
DB_NAME=blogdb
DB_USER=blog_user
DB_PASS=blog_pass

# Sesión
SESSION_SECRET=cambia_esto_por_un_secreto_seguro

# CORS — URL del frontend (en producción usar la URL de Render)
FRONTEND_URL=http://localhost:5173